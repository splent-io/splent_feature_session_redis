"""
Functional tests for splent_feature_session_redis.

This feature does not expose HTTP routes — it configures
Redis-backed sessions via inject_config.
"""


def test_placeholder():
    """Remove this once you add real functional tests."""
    assert True
